services:
  exam-system:
    build: .
    container_name: exam-system
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - ADMIN_PASSWORD=
      - UPSTREAM_STATE_URL=
